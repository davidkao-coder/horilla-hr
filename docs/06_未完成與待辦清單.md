# 06 未完成與待辦清單（Think4U HRMS）

> 接手者的 roadmap。狀態基準日：2026-07-27。

## A. 上線前必辦（安全）
| # | 項目 | 現況 | 動作 |
|---|---|---|---|
| A1 | **全員密碼** | ⚠️ 2026-07-14 測試需要，**全部 15 帳號密碼都被重設為同一組弱密碼** | 上線前逐一改強密碼，或設 SMTP 後走「忘記密碼」自助重設 |
| A2 | admin 密碼 | 同上 | 改強密碼 |
| A3 | SMTP | 未設定（通知/提醒/忘記密碼皆不能寄信）| `.env.prod` 填 EMAIL_*（建議公司信箱 App Password）|
| A4 | Google OAuth secret | client secret 曾出現在對話紀錄/下載資料夾 | GCP Console 重設 secret → 更新兩處 .env |
| A5 | ngrok authtoken | token 曾出現在對話紀錄 | ngrok 後台 rotate（僅影響手機測試）|
| A6 | 排程 | 未建立 | 依 docs/04 §4 設 crontab |
| A7 | 異地備份 | backups 只在主機本身 | rsync 到 NAS/雲端 |

## B. 部署收尾（進行中）
| # | 項目 | 現況 |
|---|---|---|
| B1 | 正式站還原資料 | 2026-07-17 部署時 DB 還原未完成（health 500→修復中，最後狀態 HTTP 000=容器啟動中）→ **接手先照 docs/04 §6 確認** |
| B2 | nginx + certbot | 尚未執行（docs/04 §2 一次性設定）|
| B3 | 路由器 80/443 → 192.168.2.135 | 待確認 |
| B4 | GPS 公司座標 | `portal_views.py` 寫死 25.007396,121.463511 待確認 |

## C. 功能缺口（要不要做由業務決定）
| # | 項目 | 說明 | 相關位置 |
|---|---|---|---|
| C1 | **審核關卡未串接**（最大缺口）| 「審核關卡管理」可設定多關卡，但請假/加班實際仍走寫死「主管→HR」；補打卡**有**吃 ApprovalWorkflow | `think4u/approval_workflow_views.py`（設定）、`leave`/`overtime_views`（未吃）；原規劃 Phase 4 |
| C2 | 產假未滿 6 月半薪 | 勞基法 §50；目前一律全薪（決策暫緩）| `payroll_rules.LEAVE_PAY_RATIO` 需改為依年資動態 |
| C3 | 補休自動轉換 | 加班核准後未自動轉補休時數 | 加班核准 hook → AvailableLeave |
| C4 | 多日請假帶時間 | 前台僅單日可填起迄時間 | `portal_views.portal_leave_submit` |
| C5 | 離職當月比例計薪 | 未實作（比照到職邏輯反向）| `monthly_attendance_views._employment` |
| C6 | 勞保到職當月按日計 | 目前整月投保 | `payroll_rules` |
| C7 | asset 側邊欄入口 | 功能可用但無選單入口 | `horilla/horilla_apps.py` SIDEBARS |

## D. 待驗證（上線前跑一輪）
1. 打卡：GPS 內/外、上下班、外勤（手機 + HTTPS 實測）
2. 請假：前台申請（含時間）→ 核准 → 餘額扣除；病假附件
3. 加班：指派 → 員工確認 → HR 核准（+通知信）
4. 補打卡：申請 → 審核 → 寫入出勤
5. 月度統計 + 薪資試算（含時薪制、投保級距、到職比例）+ Excel 對帳
6. 各角色登入看到正確頁面（admin/HR/部門主管/一般員工/force_admin_only）
7. Google SSO 正式網域實測

## E. 例行維護
- **每年**：更新 `payroll_rules.py` 勞健保級距（LABOR_GRADES/HEALTH_GRADES/費率）
- **每位新人**：`precompute_annual_leave --emp <id>` + `seed_default_leaves --emp <id>`（詳見 docs/07 SOP）
- Let's Encrypt 憑證：certbot.timer 自動續期，每 2–3 個月抽查 `sudo certbot renew --dry-run`
