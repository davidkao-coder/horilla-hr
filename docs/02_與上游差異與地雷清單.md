# 02 與上游差異 + 地雷清單（Think4U HRMS）

> 這是**踩雷經驗的結晶**。每一條都是實際發生過、耗時排查過的問題。
> 動 Horilla 原生程式前務必先讀完本文件。

## 1. Fork 與上游關係
- 上游：Horilla HRMS（官方 https://github.com/horilla/horilla-hr）
- 本 fork：davidkao-coder/horilla-hr，分支 `1.0`
- **⚠️ 不建議 merge upstream**：大量原生檔已被客製（見 §3），盲目升級幾乎必炸。若真要升級，逐檔比對 §3 清單。

## 2. 已移除 / 停用的 Horilla 模組
| 模組 | 處置 | 備註 |
|---|---|---|
| recruitment / pms / onboarding / offboarding | **完全卸載**（INSTALLED_APPS 移除）| |
| helpdesk / project / biometric / geofencing / facedetection / horilla_ldap / horilla_backup | 關閉 | |
| payroll | model 保留（migration FK 依賴）、UI 全移除 | 計薪改用 think4u 自製 |
| asset | model + 功能保留、**sidebar 無入口** | 資產類別有 autocomplete 客製 |
| horilla_documents / dynamic_fields | model 保留（強依賴）| |

## 3. 動過的原生檔案（升級高風險區）
| 檔案 | 改了什麼 |
|---|---|
| `horilla/settings.py` | zh-hant、LOGIN_URL=/landing/、Google OAuth 設定、gunicorn 相關 |
| `horilla/urls.py` | 直接 include 各 app urls（繞開 ready 時序 bug）、health、Google callback 根路徑 |
| `horilla/horilla_apps.py` | SIDEBARS / INSTALLED_APPS 精簡 |
| `base/views.py` | login_user 前後台導向邏輯（dest 選擇）、employee_workinfo_complete 欄位精簡 |
| `base/horilla_company_manager.py` | distinct 偵測加 process-wide cache（效能）|
| `employee/forms.py` | 工作資訊表單大改（薪資組成 mixin、role 單選、excel 欄位）|
| `employee/views.py` | employee_view_update 加 salary 分支、效能瘦身 |
| `attendance/views/views.py` | 出勤異常紀錄重寫、work_record_export 重寫、排除隱藏員工 |
| `leave/views.py` `leave/models.py` | 請假總覽對帳、half_paid 選項 |
| `templates/index.html` | JS 大瘦身（-4.9MB）、topbar 客製（前往前台鈕）|
| `templates/login.html` | 前台/後台選擇、Google SSO 按鈕 |
| `entrypoint.sh` | gunicorn gthread、auth migration patch 還原、compilemessages |
| `horilla/locale/zh_Hant/django.po` | 大量翻譯新增/修正 |
| `horilla_automations/*` | 中文化 + Candidate NameError 修復（recruitment 移除的副作用）|

## 4. 地雷清單（症狀 → 原因 → 正確做法）

### 🔴 M1. `attendance/views.py` 是死檔
- **症狀**：改了 `attendance/views.py` 完全沒效果。
- **原因**：`attendance/views/` 是 **package**，同名 module 被蓋掉，永遠不會被 import。
- **做法**：改 `attendance/views/views.py`。

### 🔴 M2. 原生 `Attendance` / `AttendanceLateComeEarlyOut` 表是空的
- **原因**：Think4U 的 GPS 打卡與 Excel 匯入**只寫 `AttendanceActivity`**，Horilla 原生那兩張表棄用。
- **做法**：所有出勤報表/計算一律以 `AttendanceActivity` + `think4u/attendance_compute.py` 為準。看到用原生表的原生頁面，資料對不上是正常的。

### 🔴 M3. 重建 image 後 makemigrations/migrate 全炸（NodeNotFoundError: auth.0013）
- **原因**：Horilla 用 `User.add_to_class("is_new_employee", ...)` 把欄位掛在內建 auth app，該 migration 檔存在 **site-packages 內**，rebuild image 就消失，整個 migration graph 斷掉。
- **做法**：已修——migration 檔收在 `horilla/auth_migrations_patch/`，`entrypoint.sh` 啟動時自動複製回去。**看到 NodeNotFoundError 先檢查這個**。

### 🟠 M4. Horilla 諸多 model 的 `save()` 與 `clean()` 不相容
- **症狀**：程式化建立 JobPosition / Employee 等物件時拋 TypeError。
- **做法**：批次腳本先 `Model.save = dj_models.Model.save` 還原（seed、import 命令內都有範例，搜 `_patch_save`）。

### 🟠 M5. `HorillaCompanyManager` 有 process-wide cache
- `_T4U_DISTINCT_CACHE` 以 (model_label, selected_company) 為 key。新增公司或改 company_filter 行為時要意識到 cache 存在（重啟即清）。

### 🟠 M6. 前後台分離 middleware 的路徑白名單
- `think4u/admin_gate_middleware.py` 的 `ALLOWED_PREFIXES`：新增「登入前/公開」頁面要加進去，**前綴不要帶結尾斜線**（曾因 `/logout/` vs `/logout` 踩雷）。

### 🟠 M7. 員工編輯頁欄位是「泛用迴圈」渲染
- `employee/templates/employee/create_form/personal_info_as_p.html` 用 for 迴圈吐所有表單欄位；薪資欄位（pay_type 等 10 個）在迴圈內**被明確略過**、改由 `think4u/salary/pay_type_section.html` 獨立區塊渲染。新增表單欄位時注意會不會被迴圈重複渲染。

### 🟡 M8. `.env` 有兩個層級
- 開發：`honsi-hrms/.env`（settings 直讀）+ `docker-compose.override.yaml` 的 environment（後者優先注入 OS env）。
- 正式：`.env.prod` 由 compose `--env-file` 插值傳入。改設定要想清楚在哪一層。

### 🟡 M9. compilemessages 只能編 zh_Hant
- 其他語系（fr/de）的 po 有格式 bug，全編會失敗：一律 `compilemessages -l zh_Hant`。

### 🟡 M10. po 檔加翻譯注意重複 msgid
- 同 msgid 在檔內出現兩次 → compile 直接 fatal。加新條目前先 grep。
- 另外注意共用 msgid 的語境衝突（例：`Condition` 已被譯為「狀態」，郵件自動化改用 `Trigger Condition`）。

### 🟡 M11. git-bash 的路徑轉換
- `docker compose exec` 帶 `/app/...` 參數前要 `export MSYS_NO_PATHCONV=1`。

### 🟢 M12. migrations 曾被 gitignore（2026-08-03 已修）
- **歷史**：`.gitignore` 原有 `**/migrations/**`，只 force-add 客製 migration。
  後果：**全新 clone 缺少原生 `0001_initial` 等，migration graph 直接斷掉、無法 migrate**。
  新成員接手時必須自行到 fork 點重新產生 26 個原生 migration 才能啟動（實際發生過，耗時半天）。
- **現況**：已解除該排除規則，25 個原生 migration 全部進版控（名稱經 `django_migrations` 核對，
  與生產環境完全吻合）。**新增 migration 直接 `git add` 即可，不再需要 `-f`**。

### 🟡 M13. 正式環境 DEBUG=False 的行為差異
- `SECURE_SSL_REDIRECT=True`：直接打 http 會 301（健康檢查要帶 `X-Forwarded-Proto: https`）。
- 錯誤只回 500 無 traceback → 看 `docker compose logs server`。

### 🟢 M14. 稽核 signal 讓全新 DB 的 migrate 必炸（2026-08-03 已修）
- **症狀**：對**空資料庫**執行 `migrate` 時，`auth.0001_initial` 報
  `relation "django_content_type" does not exist`（訊息完全誤導，實際與 contenttypes 無關）。
- **根因**：`MigrationRecorder` 用 ORM 寫 `django_migrations`，每套用一個 migration 就觸發一次
  `post_save`；稽核 handler 去 INSERT `think4u_auditlog`，但該表要到**第 83 個** migration
  （`think4u.0007_auditlog`）才建立。前 82 次 INSERT 都失敗——handler 雖然 `except` 接住，
  但 **PostgreSQL 的 transaction 已進入 aborted 狀態**，同一 transaction 內後續 DDL 全部 rollback。
- **修法（兩層）**：
  1. `EXCLUDED_LABELS` 加入 `"migrations.Migration"`（消除觸發源）
  2. 兩處 `AuditLog.objects.create()` 包 `with transaction.atomic()`
     → 用 savepoint 隔離，任何稽核失敗只回滾 savepoint，**不污染外層 transaction**
- **為何第 2 層重要（不只影響全新安裝）**：稽核只是附帶功能，若在正常請求中因任何原因寫入失敗
  （欄位過長、JSON 序列化、約束衝突），沒有 savepoint 會讓**使用者原本要做的那件事一起失敗**，
  且錯誤訊息指向無關的地方。這是生產環境的隱藏風險，非僅初始化問題。
- **教訓（通用）**：在 Django 裡 `except` 資料庫錯誤時，一律要用 `atomic()` 包住可能失敗的 DB 操作，
  否則等於只是把爆炸延後到同 transaction 的下一個語句。

### 🟢 M15. shell script 換行符（2026-08-03 已加防護）
- **症狀**：新機器 clone 後容器啟動失敗（`entrypoint.sh` bad interpreter）。
- **根因**：本機 `core.autocrlf=true` 把版控裡的 LF 轉成 CRLF，Linux 容器無法執行。
- **修法**：新增 **`.gitattributes`** 強制 `*.sh` / `entrypoint.sh` / `*.py` / `*.yaml` 等為 `eol=lf`
  （`.bat`/`.cmd` 保留 CRLF）。`.gitattributes` 由 repo 提供且**優先於本機 core.autocrlf**，
  不再需要每台機器手動設定。
- 若已被轉壞：`git rm --cached -r . && git reset --hard` 重新 checkout 即可。

## 5. Think4U 自製模組地圖（think4u/ app）
| 模組 | 功能 |
|---|---|
| 出勤 | GPS 打卡（portal）、出勤規則、月度統計矩陣、補打卡審核、出勤異常紀錄、Excel 匯入/匯出 |
| 計薪 | 勞健保級距、薪資組成、時薪制、投保薪資、請假扣薪、到職比例、月度試算+匯出 |
| 假勤 | 特休 Model B（週年制+遞延）、預設假別、給假申請、加班指派（主管→員工→HR）|
| 權限 | AdminAccessGroup（前後台分離/force_admin_only/acts_as_superuser）、RolePageVisibility、審核關卡管理（**尚未串進實際流程**）|
| 其他 | AuditLog 全庫稽核、Google SSO、健保眷屬明細、組織編輯（部門樹+主管）|
